import { DropdownnDirective } from './dropdownn.directive';

describe('DropdownnDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownnDirective();
    expect(directive).toBeTruthy();
  });
});
